<!--検索機能-->
<%= form_tag(search_path, :method => "get") do %>
<%= text_field(@search_content, :search_word)%>
<%= select(@search_content, :search_what, [["User","User"], ["Book","Book"]])%>
<%= select(@search_content, :search_method, [["部分一致","partial_match"], ["完全一致","perfect_match"], ["前方一致","forward_match"], ["後方一致","backward_match"]])%>
<%= submit_tag "Search" ,class: "button is-info"%> 
<% end %> 
<!--検索機能-->

<div class="container">
	<div class="row">
		<div class="col-xs-3">

			<h2>User info</h2>
			<%= render 'users/profile', user: current_user %>
			<!--メモ　右側の指定に注意。そこを間違えるとエラーが発生して表示されない -->

			<h2>New book</h2>
			<%= render 'books/newform', book: @book %>

		</div>

		<div class="col-xs-9">
			<h2>Books index</h2>
			<!--books一覧 -->
			<table class="table table-hover table-inverse">
				<thead>
					<tr>
						<th></th>
						<th>Title</th>
						<th>Opinion</th>
						<th></th>
						<th></th>
						<th colspan="3"></th>
					</tr>
				</thead>

				<tbody>
          <% @books.each do |book| %>
            <!--部分テンプレート_bookindex-->
						<tr id="book_index_<%= book.id %>">
							<%= render 'books/bookindex', book: book %>
						</tr>
						<!--部分テンプレート_bookindex-->
          <% end %>
				</tbody>
			</table>
			<!--books一覧 -->


		</div>
	</div>
</div>